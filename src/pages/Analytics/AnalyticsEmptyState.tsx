import { EmptyState } from '../../components/common/EmptyState';

export const AnalyticsEmptyState = () => {
  return (
    <EmptyState
      title="No analytics yet"
      description="Start uploading projects to see performance trends, top clips, and audience insights here."
    />
  );
};
